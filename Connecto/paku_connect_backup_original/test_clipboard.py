print("Android 10+ prevents background clipboard reading unless the app is the default IME (keyboard) or has focus.")
print("But maybe there's a workaround like using a Quick Settings Tile, Accessibility Service, or showing a persistent notification with a 'Send Clipboard' button?")
